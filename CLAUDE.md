# 🌱 골든시드(GoldenSeed) 작업 규약 — 시작 전 반드시 읽는다

## 🚨🚨 절대 착각 금지 — 진짜 관리자 위치 (2026-09-12 테리 대노, 수시간 사고)
- **골든시드 진짜 관리자 = `public/admin/index.html`** (웹, seed.blogautopro.com, 탭3개: 주문승인·회원관리·발급·연장). 관리자 기능은 **무조건 여기에** 넣는다.
- **`src/pages/AdminPage.tsx` = 퍼블리/트래픽에서 딸려온 껍데기(골든시드 안 씀). 여기 넣으면 안 됨.** "관리자=AdminPage.tsx"로 착각하면 대형 사고.
  - ⚠️ **단 하나의 예외(2026-09-12 테리 확정) = 🌱계정농사 컨트롤타워.** AdminPage(로고7탭→관리자로그인, `view="admin"`)에만 새 탭으로 넣는다. 이유: gs_accounts는 **관리자 토큰**(`publy_admin_token`, gs_seed_list RPC)으로만 읽히고, 발행 봇(naver-bot 3363)은 **앱에만** 있으며(웹 관리자는 브라우저라 봇 불가), 원터치 엔진(`runOneTouch`)도 AdminPage에 이미 있음 → "봇+관리자토큰+원터치" 3박자가 여기만 모임. **테리 전용(회원·다른 곳엔 안 보임). 엄청 디테일하게.** 컨트롤타워 본체는 별도 컴포넌트 `src/components/SeedFarmTower.tsx`로 분리(7220줄 AdminPage 비대화 방지).
- **`src/`(GoldenSeedApp) = 회원용 시딩 콘솔** (설치형 Electron 앱). 관리자 아님.
- **일 시작 첫 행동 = 메모리 `project_goldenseed.md` Read.** 코드 grep보다 먼저. 안 읽고 시작 = 규칙 위반·기만. (테리: "메모리 무조건 보고 일하라니깐 왜 어겨")

> 이 파일은 매 세션 자동 로드된다. 골든시드는 **유튜브/인스타/페북 골든아워(초기 30분) 초기시딩 엔진**이다.
> 트래픽/퍼블리(publy-traffic) 포크로 시작했고, 검증된 봇 뼈대(playwright·프록시·세션·격리·AI)를 재활용한다.
> 상세 설계·로직은 메모리 `project_goldenseed.md` + 바탕화면 `골든시드_설계도_2026-09-10.md`.

## 0. 정체성 (한 줄)
조회수 조작기가 아니라 **골든아워에 정확한 타이밍·정확한 양의 초기 참여(조회·좋아요·댓글·팔로우)를 꽂아 노출 임계선을 넘겨주는 시딩 엔진.** 넘긴 뒤엔 실유저가 유기적으로 붙는다. 업계 용어로 "초기 부스터".

## 🔴 설계 제1원칙 — 임계선(레드라인) 밑에서 자연스럽게
플랫폼은 봇을 박멸 안 하고 **"조절"**한다(engagement=매출). 밴=청소가 아니라 수위조절. 그래서 승부는 **"얼마나 많이"가 아니라 "임계선 밑에서 얼마나 자연스럽게"**다. 봇의 진짜 기술=물량이 아니라 자연스러움:
- **비율 준수**: 좋아요 조회의 3~10% · 댓글 0.5~1%(단 골든아워엔 댓글 velocity 강해 좋아요만큼 태우기도) · 팔로우는 완전 소수.
- **속도 곡선**: 0→만개 급증=레드라인. 자연 가속=통과. 골든아워 몰빵도 "진짜 터진 영상 성장곡선"처럼.
- **계정당 사람 범위 활동**: 하루 좋아요 5~10, 팔로우 몇 개. 초과=밴.
- **분산**: 계정마다 다른 프록시 IP + 핑거프린트 + 세션(연좌제 밴 방지).

## 🔴 아키텍처 (불가침)
**봇 → (페북/인스타 게이트웨이 referrer) → 플랫폼 시딩 → 골든아워 넘김 → 실유저.**
- 플랫폼에 봇 직접 꽂으면 "출처불명"으로 걸림. **게이트웨이 referrer(l.instagram.com/lm.facebook.com + 인앱UA) 경유하면 "소셜 확산 진짜 유입"으로 둔갑** → 좋은 신호.
- 조회=무계정(프록시+게이트웨이). 좋아요·댓글·팔로우=로그인 계정 필요.
- **계정 = 용도별 전용 금지, 돌려쓴다(로테이션).** 영상마다 A조회+좋아요/B댓글/C구독 역할 바꿈. "좋아요만 평생"=봇 패턴.

## 🚨 트래픽에서 계승한 불가침 (계속 반복된 실수 방지)
### A. 실행 중 작업은 탭 이동·화면 내림에 안 꺼진다
- 실제 일은 봇(브라우저 창)이 한다. 앱 탭 옮겨도·최소화해도 **계속돼야** 한다. 봇 창만 살아있으면 앱은 내려도 됨.
- 화면 상태(입력값·로그·진행률·KPI) **초기화 금지**. 실행형/데이터 컴포넌트는 `{tab===x && ...}` 조건부 렌더 **금지** → `visitedAutoTabs`+`display:none` 숨김으로 언마운트 방지.
### B. 데이터는 Supabase에 영속 + 대상별 분리 + 기간 고정 금지
- 캠페인·시딩로그·계정상태·KPI 전부 Supabase 영속(앱 껐다 켜도·업데이트해도 유지). 무제한(관리자)도 카운트 기록.
- 기간 7일 고정 금지 → 30/90/전체·기간설정. 캠페인/계정별 분리 저장·표시.
### C. 부분만 고치지 않는다 = 전수 적용 (회귀 주원인)
- 회원↔관리자 / 프론트↔봇(src↔youtube-bot·insta-bot) — **항상 양쪽 다** 고친다.
- 상태/변수 하나 고치면 그걸 읽고 쓰는 **모든 진입점(초기화·로드·클릭·저장·폴링) 앞뒤로 통일**. 반쪽 패치 금지.

## 1. 기능설명은 기본값 (매번)
모든 새 기능·수정에 눈에 잘 보이는 곳에 설명(사용법·계산법·로그인 필요여부). 좋은 기능도 활용법 모르면 죽은 기능.

## 2. 배포 전 필수 (deploy-check)
- 배포·git commit/push·"됐다" 전 **`deploy-check` 스킬 실행.** "됐다"의 3근거 = ①타입체크 통과 ②DB/RPC 실측(REST) ③흐름 추적. 없으면 "미검증"이라 솔직히.
- 타입체크: 프론트 `npx tsc --noEmit -p tsconfig.json` + 봇 `cd youtube-bot && npx tsc --noEmit`.
- 봇 로컬검증 시 포트충돌·`pkill -f dist/server.js` 금지(실행 중 봇 죽임).
- `window.prompt/confirm`은 Electron에서 안 뜬다 → 커스텀 모달만.

## 3. 리포지토리 / 인프라
- 로컬 `~/Downloads/goldenseed`. 설치형 Electron(봇이 로컬 브라우저 띄움, 웹형 불가). 웹은 관제용만 곁들임.
- 봇: `youtube-bot/`·`insta-bot/`(Playwright). 프론트 `src/`(React+Vite+Electron).
- **Supabase**: ref `nnujeovecnmestvfoiqt`, region ap-northeast-2(서울). RLS 미사용(anon+필터). 테이블 `gs_proxies`·`gs_accounts`·`gs_campaigns`·`gs_seed_logs`.
- git 커밋 이메일 `s965300111@gmail.com`/김종훈.

## 4. 작업 시작 순서 (예외 없음)
1. 관련 메모리 먼저 읽기(MEMORY.md → `project_goldenseed`).
2. TaskCreate 체크리스트부터. 하나씩, 흘리지 않기.
3. 하던 작업 완결하고 다음. 새 지시는 큐에 적고 순서대로.

## 5. 개발 로드맵 (총 만드는 순서)
STEP0 뼈대(계정풀·격리·관제) → STEP1 조회 시딩(무계정·게이트웨이·watch time·골든아워 스케줄러) → STEP2 좋아요(계정농사+워밍업) → STEP3 AI 댓글 → STEP4 팔로우+인스타 확장 → STEP5 안전가드+골든아워 자동감지.
- 유튜브 쇼츠+롱폼 둘 다 지원(탄종 분기): 쇼츠=조회 velocity+완주율, 롱폼=watch time 지속.
